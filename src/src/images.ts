import styled from "@emotion/styled";
import colors from '../interfaces/colors'

const images = (colors:colors) => {
  return Object.freeze({
    Plan: styled.img({
      width: "25vw",
      height: "25vh",
    }),
  
  });
};

export default images;
